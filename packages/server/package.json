{
  "name": "@forge/server",
  "version": "0.0.1",
  "private": true,
  "description": "Forge agent server",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "echo 'dev not yet configured'",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test 'src/**/__tests__/*.test.ts'",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@forge/shared": "workspace:*",
    "dockerode": "^5.0.1",
    "zod": "^4.4.3"
  }
}
