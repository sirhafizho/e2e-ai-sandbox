{
  "name": "@forge/sandbox",
  "version": "0.0.1",
  "private": true,
  "description": "Forge Docker sandbox image and configuration",
  "scripts": {
    "build": "echo 'sandbox build: use docker build'",
    "typecheck": "echo 'no TypeScript source'",
    "clean": "echo 'nothing to clean'"
  }
}
