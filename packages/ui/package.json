{
  "name": "@forge/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Forge web UI",
  "type": "module",
  "scripts": {
    "build": "echo 'build not yet configured'",
    "dev": "echo 'dev not yet configured'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@forge/shared": "workspace:*"
  }
}
